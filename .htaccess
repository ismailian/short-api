<IfModule mod_rewrite.c>

	# Engine and Base:
	RewriteEngine on
	RewriteBase /
	Options -Indexes

	# Directory Rules:
	RewriteRule ^index\.php$ - [L]
	RewriteRule ^(.*)$ /index.php [NC,L,QSA]

</IfModule>